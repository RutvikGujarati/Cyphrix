# Local dev: http://localhost:8080 (frontend calls backend directly)
# Production: leave EMPTY - frontend uses same-origin /api (cyphrixtech Vercel serverless)
VITE_API_BASE_URL=http://localhost:8080

# Vercel (cyphrixtech): set for OTP proxy to backend
# OTP_API_BASE_URL=https://cyphrixtech-server.vercel.app
# SMTP_USER, SMTP_PASS - required for send-email on live site
